<odoo>
    <data>
        <record model="ir.actions.act_window" id="action_purchase_requisition_to_so">
            <field name="name">Request for Quotation</field>
            <field name="res_model">purchase.order</field>
            <field name="view_mode">form,list</field>
            <field name="domain">[('requisition_id','=',active_id)]</field>
            <field name="context">{
                "default_requisition_id":active_id,
                }
            </field>
        </record>
        <record model="ir.actions.act_window" id="action_purchase_requisition_list">
            <field name="name">Request for Quotations</field>
            <field name="res_model">purchase.order</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('requisition_id','=',active_id)]</field>
            <field name="context">{
                "default_requisition_id":active_id,
                }
            </field>
        </record>

        <record id="purchase_requisition_thorium_view_form" model="ir.ui.view">
            <field name="name">purchase.requisition.inherit.form</field>
            <field name="model">purchase.requisition</field>
            <field name="inherit_id" ref="purchase_requisition.view_purchase_requisition_form"/>
            <field name="priority">8</field>
            <field name="arch" type="xml">
                <xpath expr="//form" position="replace">
                    <form string="Purchase Agreements">
                        <field name="company_id" invisible="1"/>
                        <field name="currency_id" invisible="1"/>
                        <field name="active" invisible="1"/>
                        <header>
                            <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                            <button name="%(action_purchase_requisition_to_so)d" type="action"
                                    string="Nouveau devis" class="btn-primary"
                                    context="{'default_currency_id': currency_id, 'default_user_id': user_id}"
                                    invisible="state != 'confirmed'"/>
                            <button name="action_submit" string="Soumettre" type="object" class="btn-primary" invisible="state != 'draft'"/>
                            <button name="action_confirm" invisible="state != 'submit'" string="Confirmer" type="object" class="btn-primary"/>
                            <button name="action_done"
                                    invisible="state != 'confirmed' or requisition_type == 'purchase_template'"
                                    string="Fermer" type="object" class="btn-primary"/>
                            <button name="action_draft" invisible="state != 'cancel'" string="Mettre en brouillon"
                                    type="object"/>
                            <button name="action_cancel" invisible="state not in ('draft', 'confirmed')"
                                    string="Annuler" type="object"/>
                            <field name="state" widget="statusbar" statusbar_visible="draft,submit,confirmed,done" force_save="1"/>
                        </header>
                        <sheet>
                            <div class="oe_button_box" name="button_box">
                                <button name="%(action_purchase_requisition_list)d" type="action"
                                        class="oe_stat_button" icon="fa-list-alt" invisible="state == 'draft'"
                                        context="{'default_currency_id': currency_id}">
                                    <field name="order_count" widget="statinfo" string="RFQs/Orders"/>
                                </button>
                            </div>
                            <div class="oe_title">
                                <label for="name" class="oe_inline"/>
                                <h1>
                                    <field name="name"/>
                                </h1>
                            </div>
                            <group>
                                <group>
                                    <field name="vendor_id" context="{'res_partner_search_mode': 'supplier'}"
                                           readonly="state in ['confirmed', 'done'] and requisition_type == 'blanket_order'"
                                           required="requisition_type == 'blanket_order'"/>
                                    <field name="user_id" string="Buyer" readonly="state != 'draft'"
                                           domain="[('share', '=', False)]"/>
                                    <field name="requisition_type" readonly="state != 'draft'"
                                           options="{'no_create': True}"/>
                                    <field name="currency_id" groups="base.group_multi_currency"/>
                                </group>
                                <group>
                                    <label for="date_start" string="Agreement Validity"
                                           invisible="requisition_type == 'purchase_template'"/>
                                    <div class="o_row" invisible="requisition_type == 'purchase_template'">
                                        <span>
                                            <strong>From</strong>
                                        </span>
                                        <field name="date_start" widget="date"
                                               readonly="state not in ('draft', 'confirmed')"/>
                                        <span>
                                            <strong>A</strong>
                                        </span>
                                        <field name="date_end" widget="date"
                                               readonly="state not in ('draft', 'confirmed')"/>
                                    </div>
                                    <field name="reference" placeholder="e.g. PO0025"/>
                                    <field name="company_id" groups="base.group_multi_company"
                                           options="{'no_create': True}" readonly="state != 'draft'"/>
                                </group>
                            </group>
                            <notebook>
                                <page string="Produits" name="products">
                                    <field name="line_ids" readonly="state == 'done'">
                                        <list string="Produits" editable="bottom">
                                            <field name="product_id"
                                                   domain="[('purchase_ok', '=', True), '|', ('company_id', '=', False), ('company_id', '=', parent.company_id)]"/>
                                            <field name="product_description_variants"
                                                   invisible="product_description_variants == ''"/>
                                            <field name="product_qty"/>
                                            <field name="qty_ordered" optional="show"
                                                   column_invisible="parent.requisition_type == 'purchase_template'"/>
                                            <field name="product_uom_category_id" column_invisible="True"/>
                                            <field name="product_uom_id" string="UoM" groups="uom.group_uom"
                                                   optional="show" required="product_id"/>
                                            <field name="analytic_distribution" widget="analytic_distribution"
                                                   optional="hide"
                                                   groups="analytic.group_analytic_accounting"
                                                   options="{'product_field': 'product_id', 'business_domain': 'purchase_order'}"/>
                                            <field name="price_unit"/>
                                        </list>
                                        <form string="Produits">
                                            <group>
                                                <field name="product_id"
                                                       domain="[('purchase_ok', '=', True), '|', ('company_id', '=', False), ('company_id', '=', parent.company_id)]"/>
                                                <field name="product_qty"/>
                                                <field name="qty_ordered"/>
                                                <field name="product_uom_category_id" invisible="1"/>
                                                <field name="product_uom_id" groups="uom.group_uom"/>
                                                <field name="analytic_distribution"
                                                       widget="analytic_distribution"
                                                       groups="analytic.group_analytic_accounting"
                                                       options="{'product_field': 'product_id', 'business_domain': 'purchase_order'}"/>
                                                <field name="company_id" groups="base.group_multi_company"
                                                       options="{'no_create': True}"/>
                                            </group>
                                        </form>
                                    </field>
                                    <separator string="Termes et conditions"/>
                                    <field name="description" class="oe-bordered-editor"/>
                                </page>
                            </notebook>
                        </sheet>
                        <chatter/>
                    </form>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
